Drop exported API connection profiles here as JSON.
Each frontend uses a different internal format — import manually per app.
Suggested workflow:
1. Configure a connection in SillyTavern (or Marinara / Lumiverse).
2. Export from that app.
3. Store the export here for reference / manual import into the others.